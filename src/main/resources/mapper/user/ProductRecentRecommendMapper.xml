<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.routy.routyback.mapper.user.ProductRecentRecommendMapper">

    <select id="selectRecommendedProducts"
            resultType="com.routy.routyback.dto.ProductRecentRecommendDTO"
            parameterType="map">

        SELECT
        PRDNO      AS prdNo,
        PRDNAME    AS prdName,
        PRDCOMPANY AS prdCompany,
        PRDIMG     AS prdImg
        FROM PRODUCT
        WHERE 1 = 1

        <!-- 최근 본 상품 ID 리스트 -->
        <if test="recent != null and recent.size > 0">
            AND PRDNO IN
            <foreach item="no" collection="recent" open="(" separator="," close=")">
                #{no}
            </foreach>
        </if>

        <!-- 소카테고리 필터 -->
        <if test="subcate != null">
            AND PRDSUBCATE = #{subcate}
        </if>

        ORDER BY PRDNO DESC
        FETCH FIRST 4 ROWS ONLY

    </select>

</mapper>

